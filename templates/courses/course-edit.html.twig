<!DOCTYPE html>
<html lang="en">
<head>
<!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Add</title> 
</head>
<body>

    <div class="container">
        <h2 class="m-5 text-center">Course list</h2>
        <a href="/" class="float-end btn btn-success">Go back</a>
           <form action="/{{course.id}}/update" method="POST">
                <fieldset>
                    <legend>New course info</legend>
                    <div class="mb-3">
                        <label for="name" class="form-label">Name</label>
                        <input type="text" id="name" name="name" class="form-control" placeholder="Course name" value="{{ course.name }}">
                    </div>
                    <div class="mb-3">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" name="optional" {{ course.optional ? 'checked' : '' }} id="optional">
                            <label class="form-check-label" for="optional">
                                Optional
                            </label>
                        </div>
                    </div>
                    
                    <div class="mb3">
                        <select class="form-select" aria-label="Faculty options" name="faculty_id" required>
                            <option value="">Please select a faculty</option>
                            
                            {% for faculty in faculties %}
                            <option {{ faculty.id is same as(course.id) ? 'selected' : '' }} value="{{ faculty.id }}">{{ faculty.name }}</option>
                            {% endfor %}

                        </select>
                    </div>

                    <div class="mb3">
                        <label for="professor" class="form-label">Professor</label>
                        <input type="text" name="professor" id="professor" class="form-control" value="{{ course.professor }}">
                    </div>

                    <button type="submit" class="mt-3 btn btn-primary">Submit</button>
                </fieldset>
            </form>
    </div>
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>
</html>